# The workspace tracks the latest stable toolchain (per maintainer preference).
# MSRV (1.88, set by let-chains) is declared per-crate in Cargo.toml and gated in CI.
[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
targets = ["wasm32-unknown-unknown"]
